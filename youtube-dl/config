# YouTube DL Configuration File - @humbertowoody

# Select audio format:
--audio-format "best"

# Specify ffmpeg audio quality (0 better)
--audio-quality 0

# Display progress
--console-title

# Bypass geographic restriction
--geo-bypass

# Continue on download errors such as unavailable videos on a playlist
-i

# Mark videow as watched (c'mon)
--mark-watched

# Save them under $HOME/Videos/*
-o $HOME/Movies/youtube-dl/%(title)s.%(ext)s

# Download the playlist (if the URL is from a playlist)
--yes-playlist

